{
  "name": "forms-autofill",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && rm -rf dist && vite build && vite build -c vite.content.config.ts",
    "dev": "rm -rf dist && (vite build --watch & vite build -c vite.content.config.ts --watch & wait)",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "jsdom": "^29.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
